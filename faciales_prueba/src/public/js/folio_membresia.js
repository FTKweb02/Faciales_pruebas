function myFunction() {
    // Get the checkbox
    var checkBox = document.getElementById("myCheck");
    // Get the output text
    var inputchk = document.getElementById("Folio_Membresia");

    // If the checkbox is checked, display the output text and set required attribute
    if (checkBox.checked == true) {
      inputchk.removeAttribute("disabled");
      inputchk.setAttribute("required", "required");
    } else {
      inputchk.setAttribute("disabled", "disabled");
      inputchk.removeAttribute("required");
    }
  }

  function myFunction1() {
    // Get the checkbox
    var checkBox = document.getElementById("myCheck1");
    // Get the output text
    var inputchk = document.getElementById("Folio_Membresia1");

    // If the checkbox is checked, display the output text and set required attribute
    if (checkBox.checked == true) {
      inputchk.removeAttribute("disabled");
      inputchk.setAttribute("required", "required");
    } else {
      inputchk.setAttribute("disabled", "disabled");
      inputchk.removeAttribute("required");
    }
  }

  function myFunction2() {
    // Get the checkbox
    var checkBox = document.getElementById("myCheck2");
    // Get the output text
    var inputchk = document.getElementById("Folio_Membresia2");

    // If the checkbox is checked, display the output text and set required attribute
    if (checkBox.checked == true) {
      inputchk.removeAttribute("disabled");
      inputchk.setAttribute("required", "required");
    } else {
      inputchk.setAttribute("disabled", "disabled");
      inputchk.removeAttribute("required");
    }
  }

  function myFunction3() {
    // Get the checkbox
    var checkBox = document.getElementById("myCheck3");
    // Get the output text
    var inputchk = document.getElementById("Folio_Membresia3");

    // If the checkbox is checked, display the output text and set required attribute
    if (checkBox.checked == true) {
      inputchk.removeAttribute("disabled");
      inputchk.setAttribute("required", "required");
    } else {
      inputchk.setAttribute("disabled", "disabled");
      inputchk.removeAttribute("required");
    }
  }